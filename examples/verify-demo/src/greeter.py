"""A tiny module the worked example verifies.

Kept deliberately trivial. The demo is about the verify harness proving the
work was actually done, not about the code itself.
"""


def greet(name: str) -> str:
    """Return a greeting that uses the given name."""
    return f"Hello, {name}."
